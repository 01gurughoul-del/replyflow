# Add these to .env in the phase folder (parent of whatsapp_cloud).

# AI: Groq only (get key at console.groq.com)
GROQ_API_KEY=your_groq_api_key
# GROQ_MODEL=llama-3.1-8b-instant

# WhatsApp Cloud API (from Meta App Dashboard → WhatsApp → API Setup)
WHATSAPP_ACCESS_TOKEN=your_permanent_access_token
WHATSAPP_VERIFY_TOKEN=my_verify_token_123

# Optional: For webhook signature verification (App Secret from Meta App Dashboard)
META_APP_SECRET=your_app_secret

# Style: BOT_NO_EMOJI=1 (default) to disable emojis; set 0 to allow
# BOT_NO_EMOJI=1

# Brand identity (what the bot says when asked who built it)
# BOT_BRAND=ReplyFlow by MadeReal

# Optional: PORT (default 5000)
