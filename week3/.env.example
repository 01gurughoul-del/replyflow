# Copy this to the phase folder as .env (week3 loads from parent).
# Already have: GEMINI_API_KEY

# WATI (get from wati.io → More → API Docs)
WATI_API_KEY=your_wati_api_key
WATI_API_ENDPOINT=https://live-mt-server.wati.io

# Optional: PORT for running the server (default 5000)
